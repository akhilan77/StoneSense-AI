You already integrated the StoneSense-AI dashboard overhaul from the previous prompt. Two things need fixing.

## 1. Theme isn't actually applying — debug and fix

The rendered app shows a dark navy background with washed-out, low-contrast gray text instead of the intended light "paper" background with dark "ink" text. This means the custom `stonesense-*` Tailwind color tokens aren't being generated by the build, so those utility classes are silent no-ops and the app's pre-existing dark background/text is showing through unstyled.

Check, in this order, and fix whichever is wrong:

1. Open `tailwind.config.js` and confirm the `stonesense-*` colors were added **inside** `theme.extend.colors`, not directly under `theme.colors` (the latter replaces Tailwind's entire default palette instead of adding to it).
2. Confirm `content` (or `purge`, on older configs) includes the new files — it must glob something like `./src/**/*.{js,ts,jsx,tsx}` so `pages/HospitalDashboard.tsx`, `pages/DeveloperDashboard.tsx`, `pages/Login.tsx`, `components/layout/*.tsx` are actually scanned for class names.
3. Fully restart the Vite dev server (not just hot-reload) after any `tailwind.config.js` change — config edits don't always get picked up by HMR.
4. After restarting, inspect the compiled CSS (or browser devtools) and confirm classes like `.bg-stonesense-paper`, `.text-stonesense-ink`, `.bg-stonesense-teal` actually exist in the output. If they still don't, check for a second, pre-existing Tailwind config or a conflicting global dark-theme stylesheet (e.g. `index.css` or `App.css` setting `body { background: ... }` with higher specificity) and reconcile it — our new layout should be the one source of truth for background/text color on these pages.
5. Re-render `/hospital-dashboard`, `/developer-dashboard`, and the new `/login` (below) and confirm: light sage background, white cards, dark legible text, teal/indigo accents as designed — not dark navy with faint gray text.

## 2. Replace the role-toggle button with a real login flow

Two new/updated files are in the project root (same as before — copy into matching paths):

```
frontend/src/pages/Login.tsx              (new)
frontend/src/components/layout/AppLayout.tsx   (replaces the existing one — the demo "Switch to X view" button is gone, replaced with a "Log out" link)
```

Steps:
the file directory has 2 new files login.tsx and applayout.tsx

1. Copy `Login.tsx` to `frontend/src/pages/Login.tsx`.
2. Overwrite `frontend/src/components/layout/AppLayout.tsx` with the new version.
3. In the router config, add `<Route path="/login" element={<Login />} />` and make `/login` the default landing route (redirect `/` to `/login`, or set it as the app's index route — match whatever pattern the router already uses for a default redirect).
4. `Login.tsx` has two entry points:
   - **Hospital login** → shows a hospital dropdown (reuses `HospitalContext`) → "Continue" navigates to `/hospital-dashboard`.
   - **Developer login** → "Continue" navigates directly to `/developer-dashboard` (no credentials yet, matches current project stage).
5. Confirm the old role-toggle button is gone from both dashboards' headers, replaced by "Log out", which navigates back to `/login`.
6. The hospital dropdown in the dashboard header (for switching hospitals without logging out) stays as-is — only the role-switching button is removed.

No authentication logic is being added yet — this is purely the navigation/UX restructure the two login entry points require. Report back with files changed and confirm the theme now renders correctly on all three pages (`/login`, `/hospital-dashboard`, `/developer-dashboard`).
